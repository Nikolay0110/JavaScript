export const clone = function (obj) {
  let copy_object = Object.assign({} ,obj)
    return copy_object;
    /**
   * Необходимо реализовать логику, которая для входного
   * объекта сделает копию. И вернёт эту копию в качестве
   * результата.
   */
};
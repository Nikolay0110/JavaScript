import { phoneBook } from "./phoneBook.js";
import { deleteContact } from "./deleteContact.js";

/**
 * Здесь можно написать логику что будет вызывать
 * функцию deleteContact, а также можно добавить
 * выводы в консоль чтобы видеть результат и убедиться
 * в том что deleteContact работает как ожидается.
 */